# Assignment 10 — Bootstrap (Solutions)
Each folder contains a separate HTML and CSS file as required. All pages use **Bootstrap 5 via CDN**, semantic tags, and responsive layouts.

Open any `index.html` in your browser using a local server (or directly). No custom JS is used beyond Bootstrap's bundle for components like Carousel.
